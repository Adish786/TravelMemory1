apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: travel-memory
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer   # or NodePort depending on your cluster
